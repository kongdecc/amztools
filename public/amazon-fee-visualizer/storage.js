const STORAGE_BASE={standard:{off:.78,peak:2.40},bulky:{off:.56,peak:1.40},hazStandard:{off:.99,peak:3.63},hazBulky:{off:.78,peak:2.43}};
const STORAGE_UTIL_LIMITS=[22,28,36,44,52,Infinity];
const STORAGE_UTIL_STANDARD=[0,.44,.76,1.16,1.58,1.88];
const STORAGE_UTIL_BULKY=[0,.23,.46,.63,.76,1.26];
function storageHTML(){return `<h3>月度库存仓储费</h3><p class="card-intro muted">按商品尺寸、月平均库存量和仓储利用率估算基本仓储费及适用的利用率附加费。</p><div class="grid"><div class="field"><label for="storage-season">仓储费时段</label><select id="storage-season"><option value="off">非旺季（1～9 月）</option><option value="peak">旺季（10～12 月）</option></select></div><div class="field"><label for="storage-month">计费月份</label><select id="storage-month">${Array.from({length:12},(_,i)=>`<option value="${i+1}">${i+1} 月</option>`).join('')}</select></div><div class="field"><label for="storage-tier">尺寸分类</label><select id="storage-tier"><option value="standard">标准尺寸</option><option value="bulky">大件</option></select></div><div class="field"><label for="storage-haz">危险品</label><select id="storage-haz"><option value="no">否</option><option value="yes">是</option></select></div><div class="field"><label for="storage-qty">月平均每日库存 · 件</label><input id="storage-qty" type="number" min="0" step="any" placeholder="例如 100"></div><div class="field"><label for="storage-volume">单件包装体积 · 立方英尺</label><input id="storage-volume" type="number" min="0" step="any" placeholder="例如 0.05"></div><div class="field"><label for="storage-weeks">仓储利用率 · 周</label><input id="storage-weeks" type="number" min="0" step="any" placeholder="例如 25"></div><div class="field"><label for="storage-oldqty">存放超过 30 天的平均每日库存 · 件</label><input id="storage-oldqty" type="number" min="0" step="any" placeholder="例如 10"></div><div class="field"><label for="storage-qualified">附加费资格</label><select id="storage-qualified"><option value="no">不满足全部条件</option><option value="yes">满足全部条件</option></select></div></div><div class="actions"><button class="primary" id="storage-calculate">计算月度仓储费</button></div><div id="storage-error" class="error" role="alert"></div><div id="storage-results" class="results" aria-live="polite"></div><h4 class="table-title">基本月度仓储费率</h4><div id="storage-base-table"></div><h4 class="table-title">仓储利用率附加费率</h4><div id="storage-util-table"></div><details><summary>计算说明</summary><p>基本仓储费 = 月平均每日库存件数 × 单件包装体积（立方英尺）× 基本月费率。利用率附加费 = 存放超过 30 天的平均每日库存件数 × 单件体积 × 对应附加费率。</p><p>仓储利用率附加费仅在以下条件同时满足时适用：专业销售账户；向亚马逊运营中心发送首件商品已超过 365 天；对应尺寸分段的库存平均每日占用体积至少 25 立方英尺；仓储利用率超过 22 周。符合新卖家或个人计划豁免时选择“不满足全部条件”。</p><p>月度费用通常在费用发生月份之后收取。危险品使用独立基本费率；危险品缺少可用的独立利用率附加费表，因此不计算该项。</p></details>`;}
function storageTables(){const baseRows=[['非危险品标准尺寸',.78,2.40],['非危险品大件',.56,1.40],['危险品标准尺寸',.99,3.63],['危险品大件',.78,2.43]].map(row=>`<tr><td>${row[0]}</td><td>${fbaMoney(row[1])}</td><td>${fbaMoney(row[2])}</td></tr>`).join('');document.getElementById('storage-base-table').innerHTML=`<div class="table-wrap"><table class="rate-table"><thead><tr><th>商品分类</th><th>1～9 月 / 立方英尺</th><th>10～12 月 / 立方英尺</th></tr></thead><tbody>${baseRows}</tbody></table></div>`;const labels=['≤22 周','>22～28 周','>28～36 周','>36～44 周','>44～52 周','>52 周'];const utilRows=labels.map((label,i)=>`<tr><td>${label}</td><td>${fbaMoney(STORAGE_UTIL_STANDARD[i])}</td><td>${fbaMoney(STORAGE_UTIL_BULKY[i])}</td></tr>`).join('');document.getElementById('storage-util-table').innerHTML=`<div class="table-wrap"><table class="rate-table"><thead><tr><th>利用率</th><th>标准尺寸 / 立方英尺</th><th>大件 / 立方英尺</th></tr></thead><tbody>${utilRows}</tbody></table></div>`;}
function storageEstimate(month,tier,haz,qty,volume,weeks,oldQty,qualified){const key=haz?(tier==='standard'?'hazStandard':'hazBulky'):tier,baseRate=STORAGE_BASE[key][month>=10?'peak':'off'],base=Math.round(qty*volume*baseRate*100)/100;let utilRate=0;if(!haz&&qualified&&weeks>22){const index=STORAGE_UTIL_LIMITS.findIndex(limit=>weeks<=limit);utilRate=(tier==='standard'?STORAGE_UTIL_STANDARD:STORAGE_UTIL_BULKY)[index];}const surcharge=Math.round(oldQty*volume*utilRate*100)/100;return{baseRate,utilRate,base,surcharge,total:Math.round((base+surcharge)*100)/100};}
function initStorage(){const month=document.getElementById('storage-month'),season=document.getElementById('storage-season'),currentMonth=new Date().getMonth()+1;month.value=String(currentMonth);season.value=currentMonth>=10?'peak':'off';season.addEventListener('change',()=>{month.value=String(season.value==='peak'?(currentMonth>=10?currentMonth:10):(currentMonth<=9?currentMonth:9));month.dispatchEvent(new Event('input',{bubbles:true}));});month.addEventListener('input',()=>{season.value=Number(month.value)>=10?'peak':'off';});document.getElementById('storage-calculate').addEventListener('click',calculateStorage);document.querySelectorAll('#content input,#content select').forEach(el=>el.addEventListener('input',()=>document.getElementById('storage-results').classList.remove('visible')));storageTables();}
function calculateStorage(){const month=Number(document.getElementById('storage-month').value),tier=document.getElementById('storage-tier').value,haz=document.getElementById('storage-haz').value==='yes',qualified=document.getElementById('storage-qualified').value==='yes',ids=['storage-qty','storage-volume','storage-weeks','storage-oldqty'],values=ids.map(id=>{const s=document.getElementById(id).value.trim();return s===''?NaN:Number(s)}),error=document.getElementById('storage-error'),out=document.getElementById('storage-results');if(values.some(x=>!Number.isFinite(x)||x<0)||values[1]===0||values[3]>values[0]){error.textContent='请填写非负数；单件体积须大于 0，超过 30 天的库存不得超过月平均库存。';out.classList.remove('visible');return;}error.textContent='';const [qty,volume,weeks,oldQty]=values,result=storageEstimate(month,tier,haz,qty,volume,weeks,oldQty,qualified);out.innerHTML=`<div class="feature"><div><span class="label">预计月度仓储费</span><strong>${fbaMoney(result.total)}</strong><p>${month} 月 · ${haz?'危险品':'非危险品'} · ${tier==='standard'?'标准尺寸':'大件'}</p></div></div><div class="metrics"><div class="metric"><span>基本仓储费</span><b>${fbaMoney(result.base)}</b><br><small>${fbaMoney(result.baseRate)}/立方英尺</small></div><div class="metric"><span>仓储利用率附加费</span><b>${fbaMoney(result.surcharge)}</b><br><small>${fbaMoney(result.utilRate)}/立方英尺</small></div><div class="metric"><span>月平均占用体积</span><b>${fmt(qty*volume)} <small>立方英尺</small></b></div></div><div class="trace">基本费：${fmt(qty)} × ${fmt(volume,4)} × ${fbaMoney(result.baseRate)} = ${fbaMoney(result.base)}。附加费：${fmt(oldQty)} × ${fmt(volume,4)} × ${fbaMoney(result.utilRate)} = ${fbaMoney(result.surcharge)}。</div>`;out.classList.add('visible');}
